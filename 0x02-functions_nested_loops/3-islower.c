#include "main.h"

/**
 * _islower - print alphabet
 * check if those alphabets are in lowercase or not
 *
 * Return: Always 0.
 *
 */

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
